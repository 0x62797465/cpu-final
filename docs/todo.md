# Misc
Add UART to a certain address and testbench

# Testbench CPU.sv with realish programs # Well it's kinda happening?

# Update testbenches, currently broken: // due to rapidly changing ports/behavior as we port to UART compatability and as numerous bugs are fixed, CPU will take priority over the rest until the tail end
decode
issue
cpu
agu

# FPGA!

# Bonus
Make the memory unit have a 1-cycle delay instead of 2 (LSQ stuff).
Make oob reads/writes actually fault
Rewrite rename testbench
